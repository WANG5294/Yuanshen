## 板载接线（无外接导线）

### 电源
```
VCC(3.3V) ------ J1排针全部引脚(1~8)
+5V       ------ J2排针全部引脚(1~8)
GND       ------ J3排针全部引脚(1~8)
```

### USB 转串口（CP2102N，板载）
```
Micro-USB VBUS ------ F1(保险丝) ------ D1(防反接二极管) ------ BAT
CP2102N TXD  ------ R14(串联) ------ GPIO1(U0TXD)
CP2102N RXD  ---------------------- GPIO3(U0RXD)
CP2102N DTR  ------ Q2(NPN) ------ GPIO0（自动下载：拉低进入下载模式）
CP2102N RTS  ------ Q1(NPN) ------ CHIP_PU（自动下载：复位控制）
```

### 按键
```
VCC(3.3V) ------ R12(1kΩ上拉) ------ GPIO35 ------ KEY1按键(K4) ------ GND
                                      (KEY1按下时 GPIO35 接 GND)

VCC(3.3V) ------ R9(1kΩ上拉)  ------ GPIO34 ------ KEY2按键(K3) ------ GND
                                      (KEY2按下时 GPIO34 接 GND)

GPIO0     --------------------------- BOOT按键(K1) ------ GND
CHIP_PU   --------------------------- RESET按键(K2) ------ GND
```

### LED
```
VCC(3.3V) ------ 红色LED(LED3，阳极)
                 红色LED(LED3，阴极) ------ R11(限流) ------ GPIO33
                 (低电平点亮：GPIO33=0 亮，GPIO33=1 灭)

VCC(3.3V) ------ 绿色LED(LED2，阳极)
                 绿色LED(LED2，阴极) ------ R10(限流) ------ GPIO32
                 (低电平点亮：GPIO32=0 亮，GPIO32=1 灭)

VCC(3.3V) ------ R3(限流) ------ 红色电源指示LED(LED1，阳极)
                                 红色电源指示LED(LED1，阴极) ------ GND
                 (通电常亮，不可编程控制)
```

### 说明
- KEY1按键对应板上丝印 K4，KEY2按键对应板上丝印 K3。
- BOOT按键对应板上丝印 K1（按下时 GPIO0 接 GND，低电平进入下载模式）。
- RESET按键对应板上丝印 K2（按下时 CHIP_PU 接 GND，硬件复位），不可编程。
- LED3(红色)与 LED2(绿色)为用户可编程 LED，均低电平点亮：GPIO 输出 0 亮、输出 1 灭。
- LED1(红色)为电源指示灯，通电常亮，不可编程控制。
- GPIO34/GPIO35 为仅输入引脚，无内部上拉/下拉，板上已外接 1kΩ 上拉至 VCC(3.3V)。
- UART0（GPIO1=TX, GPIO3=RX）经 CP2102N 提供 USB 串口，MicroPython REPL 默认占用，默认参数 115200-8-N-1。
- 无其他外接导线。
