## 板载接线

### 电源
```
VCC(3.3V) ------ J1排针全部引脚(1~8)
+5V       ------ J2排针全部引脚(1~8)
GND       ------ J3排针全部引脚(1~8)
```

### 按键
```
VCC(3.3V) ------ R12(1kΩ上拉) ------ GPIO35 ------ 开关1(KEY1,K4) ------ GND
|                                     (按下时 GPIO35=0，释放时 GPIO35=1)
|
|             R9(1kΩ上拉)  ------ GPIO34 ------ 开关2(KEY2,K3) ------ GND
|                                     (按下时 GPIO34=0，释放时 GPIO34=1)
|
|             R12(1kΩ上拉) ------ GPIO35 ------ J5_7/8(排针引出)
|             R9(1kΩ上拉)  ------ GPIO34 ------ J5_9/10(排针引出)

GPIO0     --------------------------- BOOT按键(K1) ------ GND
CHIP_PU   --------------------------- RESET按键(K2) ------ GND
```

### LED
```
VCC(3.3V) ------ 红色LED1(LED3,阳极)
|                红色LED1(LED3,阴极) ------ R11(限流) ------ GPIO33 ------ J5_11/12(排针引出)
|                (GPIO33=0 亮，GPIO33=1 灭)
|
|             绿色LED1(LED2,阳极)
|               绿色LED1(LED2,阴极) ------ R10(限流) ------ GPIO32 ------ J5_13/14(排针引出)
|               (GPIO32=0 亮，GPIO32=1 灭)
|
|             R3(限流) ------ 红色电源指示LED(LED1,阳极)
                                红色电源指示LED(LED1,阴极) ------ GND
                (通电常亮，不可编程控制)
```

### USB 转串口（CP2102N，板载）
```
Micro-USB VBUS ------ F1(保险丝) ------ D1(防反接二极管) ------ BAT
CP2102N TXD  ------ R14(串联) ------ GPIO1(U0TXD)
CP2102N RXD  ---------------------- GPIO3(U0RXD)
CP2102N DTR  ------ Q2(NPN) ------ GPIO0（自动下载：拉低进入下载模式）
CP2102N RTS  ------ Q1(NPN) ------ CHIP_PU（自动下载：复位控制）
```

### 关键约束
- GPIO34/GPIO35 为仅输入引脚，不可配置为输出。
- 红色LED1(LED3)与绿色LED1(LED2)均为共阳接法，低电平点亮。
- LED1(红色)为电源指示灯，通电常亮，不可编程控制。
- UART0（GPIO1=TX, GPIO3=RX）经 CP2102N 提供 USB 串口，MicroPython REPL 默认占用。
- 无其他外接导线。
