## 板载接线（无外接导线）

### 电源
- VCC(3.3V) --- J1排针全部引脚
- +5V --- J2排针全部引脚
- GND --- J3排针全部引脚

### 按键
- VCC(3.3V) --- R12(1kΩ) --- GPIO35 --- K4(K1按键) --- GND
- VCC(3.3V) --- R9(1kΩ)  --- GPIO34 --- K3(K2按键) --- GND

### LED
- VCC(3.3V) --- LED3(红色阳极) --- LED3(红色阴极) --- R11(限流) --- GPIO33
- VCC(3.3V) --- LED2(绿色阳极) --- LED2(绿色阴极) --- R10(限流) --- GPIO32

### 说明
- KEY1对应板上K4，KEY2对应板上K3。
- LED3为红色，LED2为绿色，均低电平点亮。
- GPIO34/35为仅输入引脚，板上已外接1kΩ上拉至VCC。
- 无其他外接导线。
