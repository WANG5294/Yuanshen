# 第3周汇报：Yuanshen Agent 在 mini Claude Code 基础上的改造

## 一、面向硬件开发的聊天框命令入口

mini Claude Code 提供了“模型理解任务—调用工具—返回结果”的通用 Agent 基础，但直接
用于 ESP32 时，用户往往不知道当前串口、开发板、麦克风、Skill 和工具是否可用，也难以
判断失败来自程序、接线还是运行环境。Yuanshen 因此在聊天输入框中增加了一组斜杠命令，
把环境状态检查从 Agent 循环中独立出来。输入 `/work` 可以检查串口设备、`mpremote`、
开发板 REPL、上传与运行能力、麦克风通道、当前音频验收模式、Skill 数量、任务目录和
大模型 API；输入 `/tool` 可以分别查看本地工具与 MCP 工具，了解模型能读写哪些文件、
能否烧录、运行或录音；输入 `/skill` 可以查看已经拆分并可按需加载的硬件知识及历史
经验；输入 `/audio`、`/audio on`、`/audio off` 或 `/audio required` 可以在自动降级、
关闭和严格音频验收之间切换；此外 `/wiring` 用于查看当前接线，`/doc` 用于导入符合
规范的新硬件知识文档。

这些命令不会直接启动任务循环，也不会消耗多轮工具调用。用户可以先确认“板子是否在线、
麦克风是否有信号、Agent 有哪些能力”，再决定如何描述任务。它把原本隐藏在程序内部的
运行条件变成可观察状态，减少模型在设备未连接时反复尝试，也便于课堂展示和故障定位。
对硬件 Agent 来说，这种入口的意义是建立清晰的人机边界：用户负责确认真实环境，Agent
负责在已知能力内执行，从而提高任务启动前的确定性、透明度和可解释性。

## 二、循环外完成需求与接线规范化

用户首先用 `nano` 等编辑器在 `wiring.md` 中记录真实接线，再在聊天框输入当前任务。
Yuanshen 不会立刻进入主体 Agent 循环，而是先启动一个独立的需求规范化模型。该模型
同时阅读三类信息：用户在聊天框中的自然语言目标、用户确认的 `wiring.md`、完整的
ESP32 板级开发手册。三者的职责严格区分：聊天内容说明“要实现什么”，接线文档说明
“现实中怎样连接”，开发手册提供板载器件、GPIO、驱动电路、有效电平和电气限制等硬件
事实。模型据此输出一份完整、详细、可测试的任务要求，以及名称统一、节点清晰、没有
歧义的规范化接线文档；如果接线与手册冲突，则明确列为待确认事项，而不是静默修改。
用户可以输入确认、取消，也可以直接给出修改意见重新生成。只有用户确认后，程序才原子
更新 `wiring.md`、保存 `requirement.md`，并从 0 秒开始进入正式循环。

确认后的需求与接线会被冻结在本工程的 System Prompt 中，在整个 Agent 循环内保持
不变；ESP32 开发手册全文则留在循环外，不重复占用每一轮上下文。这个设计解决了自然
语言任务常见的遗漏和歧义，例如用户只写“按键控制蜂鸣器”，规范化阶段会结合接线和
手册补全器件名称、GPIO、有效电平、输入输出状态、验收方式及不能确定的参数。它还避免
模型在后续轮次中自行改变接线假设，使代码、烧录和测试始终围绕同一份经用户确认的事实
推进。将“理解任务”与“执行任务”分开，也使主体循环更稳定、更容易命中提示词缓存，
并为审计留下明确基线：最终失败究竟来自需求、接线、代码还是设备证据，都可以追溯。

## 三、Skill 来源，以及 MCP 工具与本地工具的分工

Yuanshen 的硬件 Skill 主要从 ESP32 开发文档中按主题拆分而来，例如 GPIO 能力、
ADC/触摸输入、PWM/DAC/RMT、SPI/I²C/UART、深度睡眠和板级器件约束。System Prompt
只保存 Skill 的名称和用途，模型遇到匹配任务时再调用 `Skill` 工具读取对应正文。相比
每轮都放入整本手册，这种渐进加载减少上下文长度和无关信息干扰，也更容易维护：硬件
知识更新时只修改对应分块，历史任务中总结出的特殊经验还可以作为新的 `exp-*` Skill
继续扩充知识库。

工具分为本地工具和 MCP 工具。本地工具包括 `read_file`、`write_file`、`edit_file`，
负责读取工程、生成和修改 MicroPython 程序；受限的 `bash` 用于语法检查与测试；
`TodoWrite` 维护“编写代码、烧录代码、测试代码、完成”四项主线；`Skill` 负责加载知识。
MCP 工具连接真实硬件：`list_ports` 查看串口，`upload` 上传板上 `main.py`，
`repl_exec` 和 `run_script` 执行测试，`device_ls`、`device_rm`、`soft_reset` 管理板端
状态；`mic_check`、`record_audio`、`play_and_record`、`analyze_wav` 和
`compare_audio` 构成真实发声—麦克风接收—数字分析的音频闭环。两类工具分工后，本地
文件操作与外部设备操作拥有不同边界和证据，既提高安全性，也让失败位置更容易判断。

## 四、每轮循环以工具意图和真实执行为中心

在每一轮循环中，大模型不会把一段代码直接显示在终端后就宣称任务完成。它首先输出约
30～50 个汉字的“本轮目标”，说明即将调用什么工具、调用目的是什么；程序随后显示轮次、
从用户确认后累计的持续时间、工具名称及工具类别。工具执行完成后，同一状态行更新为
成功、失败或跳过，下一轮开始时模型能够看到上一轮的完整工具结果和 Todo 变化。

这一设计突出 Agent 与普通聊天模型的差别：代码必须通过 `write_file` 真正写入任务
目录，通过 MCP 的 `upload` 真正成为板上的 `main.py`，再通过明确执行板上
`main.py` 的命令获得实机证据。声音任务还可以调用麦克风工具，把真实录音转换为 RMS、
峰值、基频、音分偏差和包络相关性等大模型可读取的结构化数据。遇到具体外设时，模型先
调用对应 Skill 获取硬件规则，再决定 GPIO 配置和测试步骤。每轮快照会保存 System
Prompt、消息历史、工具定义、调用参数、工具结果和 TodoList，因此“模型为什么调用这个
工具、工具实际做了什么、任务为什么成功或失败”都能够复查。这样的流程把注意力从
“生成代码文本”转向“使用知识、操作设备并取得证据”，更符合真实嵌入式开发。

## 五、任务结束后的经验提取与 Skill 沉淀

任务输出完成后，Yuanshen 会读取本次流程记录，判断是否出现值得复用的特殊经验，并
生成候选 Skill。经验必须来自真实过程，说明适用情形、现象、原因和处理步骤，而且保存
前需要用户确认。例如一次警笛任务中，板上程序和 PWM 输出都正常，但虚拟机麦克风录音
质量差；系统可以总结出“先做麦克风基线与固定音阳性对照，音频通道异常时区分设备执行
通过和音频未验证，避免判定整个任务失败”的经验。下次遇到类似任务，Agent 可以按需
加载该 Skill，直接采用已经验证过的排查顺序。这样既不会把偶然失败无条件写入知识库，
又能让真实调试经验逐步积累，使 Agent 越来越适合当前开发板、工具链和实验环境。
