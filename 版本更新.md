# 版本更新

## 2026-07-26｜v1.0.0 正式版

- 将 v4.2 开发线冻结为首个正式发布基线，应用显示版本改为 `1.0`，npm 包版本改为
  `1.0.0`；历史版本记录继续保留，便于追溯功能来源。
- 正式版保留循环外需求/接线规范化、固定 System Prompt、四项 TodoList、逐轮审计、
  ESP32 实机工具和可切换音频验收。
- 完成主程序、MCP 客户端、工具执行、音频门禁、输入交互、归档与启动器的全代码审查；
  10 项待处理风险及建议见
  [`docs/code-audit-v1.0.md`](docs/code-audit-v1.0.md)。
- 后续完成 10 项审查问题的代码修复：限制 Shell 执行、收紧 MCP 文件路径、强化
  `main.py` 实机证据、区分音频跳过、结构化音频需求与指标、改进 MCP 超时/诊断/消息
  分发，并禁止 Todo 状态回退。
- 新增《第3周汇报》，总结 Yuanshen Agent 相对 mini Claude Code 基线在命令入口、
  循环外需求规范化、Skill/MCP、本轮工具意图和经验沉淀方面的改造及意义。

## 2026-07-26｜v4.2

- 循环外规范化模型读取 ESP32 硬件手册、`wiring.md` 和聊天任务；用户确认后，将规范化任务要求与规范化接线一起冻结进工程级 System Prompt，手册全文不进入主体循环。
- User/Assistant/Tool 历史保持追加式稳定前缀；TodoList 固定在输入末尾，且严格只含“编写代码、烧录代码、测试代码、完成”四项。
- 每轮工具开始时实时输出轮次、累计时间、工具与约 50 字目的，工具结束后在同一终端行更新成功或失败。
- 需求与接线规范化同时读取 ESP32 板级硬件参考手册和 `wiring.md`；允许用手册补全
  已识别板载器件的 GPIO、驱动与有效电平，同时保持用户实际外接拓扑优先并显式报告冲突。
- 硬件参考读取与主体 Agent 循环彻底分离：循环外的前置模型读取硬件手册、
  `wiring.md` 和用户要求，确认后生成 `requirement.md` 并更新 `wiring.md`；主体
  Agent 不携带硬件手册全文，只使用确认后的文档和按需 Skill。
- 音频验收新增 `auto|required|off` 三种模式；默认 `auto` 在一次闭环失败后把音频降级
  为警告并停止重试，不再让坏录音拖垮已通过设备执行验证的整个任务。
- 输入栏新增 `/audio`，可在当前会话交互选择音频开关，也支持
  `/audio on|off|required` 直接切换。
- `off` 和已降级的 `auto` 会在程序层跳过后续麦克风工具；最终报告必须区分设备执行通过
  与音频未验证，禁止把降级写成音频通过。
- 音频闭环 Skill 增加扫频/警笛验证方法：累计相位生成 preview、短时频率轨迹、
  匹配基线 A/B、削波检查、固定音阳性对照以及 Capture 增益恢复。
- npm 包版本升级为 `4.2.0`。

## 2026-07-26｜v4.1

- 用户确认规范化需求与接线后从 0 秒开始计时，持续时间改用单调时钟。
- 终端输入接入 `prompt_toolkit`，改善中文输入时退格删除、光标定位和残留空格。
- 工具目的缺失时明确显示“模型未声明”，不再由程序生成泛化目的。
- TodoList 增加程序级顺序、当前烧录版本和实机验证门禁；代码修改或重新烧录会使旧验证证据失效。
- 声音任务只有在峰值/噪声底比 ≥5 且基频偏差在 ±10 音分内时，才允许完成测试和任务。
- npm 包版本升级为 `4.1.0`。
- v4.1 发布前实机麦克风测试已执行：板端发声调用和录音成功，但峰值/噪声底比为 1.52、检测基频为 58.85 Hz，未达到 440 Hz 闭环验收标准，因此如实记录为验证失败。

## 2026-07-25｜v4.0

- Prompt 重构为固定 System Prompt、追加式 User/Assistant/Tool 历史和末尾动态 TodoList。
- System Prompt 完整承载身份、模型要求、Skill、Tool、MCP/文件/硬件接线、安全边界、验证规则和审计要求，并在单任务内冻结。
- TodoList 固定为最新 User 内容的最后一段；当前版本变化只影响输入尾部。
- 每轮结束在 `file/<任务>/rounds/round-NNNN.md` 保存完整 System、工具定义、全部消息历史、模型响应、工具结果与 TodoList。
- 屏幕输出统一为“轮次、累计秒数、工具、成功/失败”状态行和“作用和目的”说明行。
- 新任务根目录由 `files/` 调整为 `file/`；原 `files/` 作为 v3 历史数据保留，不自动迁移或删除。
- 主程序统一命名为 `yuanshen.py`，npm 包版本升级为 `4.0.0`。
- 文档集中整理到 `docs/`。

## 2026-07-24｜v3.1

- 普通任务增加“需求与接线规范化确认”门禁：先完整展示严谨需求和接线草案，再由用户确认、修改或取消。
- 用户确认前不创建 Todo、任务目录或 Agent 消息链，原始提示词不会进入 Agent 循环。
- 用户确认后，以规范化需求替换原始任务输入，并原子覆盖 `wiring.md`；本次 Agent 的 System Prompt 使用确认后的接线快照。
- 规范化结果使用稳定器件命名、明确输入/输出状态和文本接线图；不确定或危险信息标为待确认，不允许静默猜测。
- npm 包版本更新为 `3.1.0`，同步更新 README、Prompt 结构和用户使用文档。

## 2026-07-23

- 将项目根目录下的 `xiaohong/` 更名为 `Yuanshen/`。
- 清理原有任务对话、流程报告、经验提取记录、测试录音和板端代码备份；`skills/` 完整保留。
- 修改任务保存规则：每次 Agent 执行结束后，只在任务目录中保留最终代码文件和未经摘要、未经截断的完整 `userprompt.md`。
- 过程日志仅在当前任务内存中用于运行和经验提取，不再写入磁盘。
- 覆盖板端 `main.py` 时不再保存历史备份。
