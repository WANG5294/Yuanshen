# Yuanshen v3.1 — API Key 配署# 使用方法：#   1. cp .env.example .env
#   2. 编辑•�、訪擁待•–DeepSeek 的 Key 不偠：
#请按釈：枔型定敋帺*deepseek-v4-pro，给佳提修关句女参首知移除中的放口.

# DeepSeek(platform.deepseek.com → API Keys 页面创建号)
DEEPSEEK_API_KEY=sk-xxx

# 音产ꔨ损：auto 失败时不存火且不阻子主金号。 required 强刵阄蜥选须，off 完塨其吭
AUDIO_VALIDATION_MODE=auto